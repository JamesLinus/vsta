#
# ld.db
#	A pseudo-shlib, the secondary shlib loader
#
# This participates as a shared library so it can get its piece
# of the address space during assignment.  It should never be
# explicitly linked against.  load2() is provided as a visible
# entry for form's sake only.
#
library ld.a
__load2
__mmap_shl hidden
__notify_shl hidden
_msg_connect_shl hidden
_msg_disconnect_shl hidden
_msg_port_shl hidden
_msg_send_shl hidden
_munmap_shl hidden
